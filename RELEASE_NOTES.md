# OTZ V5.3.6 Release Notes

- 司機端新增註冊按鈕，司機註冊後資料會直接寫入後台司機名單並可登入。
- 新增東港鎮內固定車資：上下車地點都位於屏東縣東港鎮時，估價一律為 150 元。
- 私訊與群組統一採用精準介入條件，一般聊天不回覆、不建單、不呼叫 Google API。
- 攔截「我家／某某家」等不可導航暱稱，以及國外、外島地名。
- 單獨輸入東山、星光、大東港等已知東港地標仍可直接派單。
- 完整路線必須至少包含一個可辨識行政區、地址或地標。
- Google `partial_match` 增加名稱、路名與門牌相關性驗證，避免強制配到錯誤的東港地點。

## V5.3.5

- 修正「兩個人／2個人」被殘留在下車地點、且人數顯示尚未提供的問題。

## V5.3.4

- 新增「星光／星光KTV／東港星光大道KTV」別名，定位至屏東縣東港鎮新勝街107號2樓。
- 新增「大東港／大東港釣蝦場」別名，定位至屏東縣東港鎮水源路63-7號。
- 無縣市且地點不明確時優先以屏東縣東港鎮搜尋；若地圖回傳外縣市仍會拒絕。

## V5.3.3

- 新增東港在地地標「東山／東山KTV／東山視聽歡唱」校正，固定解析為屏東縣東港鎮明德路37號。
- 支援「東山要一台車」「東山KTV一台車」等自然叫車說法，不再把「一台車」誤當地址。

## V5.3.2

- 放寬 Google `partial_match`：結果位於台灣本島且縣市吻合即可接受。
- 高雄、屏東地點可省略縣市；其他地區必須明確輸入縣市名稱。
- 保留縣市衝突、國外與外島攔截，避免派到錯誤地區。
- 人數支援句首、句中、句尾、中文數字及「大／小」格式，且不再破壞地點文字。
- 只有上車地點、沒有目的地時仍會直接建立待接訂單，不呼叫 Google 路線 API。
- 缺少目的地的訂單在管理端與司機端顯示「車資待確認」，並隱藏目的地導航。

## V5.3.1

- 修正「人數2位」殘留在人名或地點欄位而造成地點無法解析。
- 支援 `2位`、`人數：2位`、`2位乘客`、`2大1小` 等乘客格式。
- 新增高雄長庚醫院地標校正（高雄市鳥松區大埤路123號）。

- 新增 LINE 群組安靜模式。
- 支援中文數字、相對日期、斜線日期及24小時制時間。
- 修正日期斜線混入上車地點與時間只顯示 `08`。
- 新增立即單／預約單前後30分鐘分類規則。
- 接受經校正的台灣縣市、鄉鎮簡稱及林邊分駐所。

- 後台派單改用畫面選擇編號映射真實司機 ID，避免輸入帳號或姓名造成 `DRIVER_NOT_FOUND`。
- 後端新增訂單 ID、司機 ID 與車資格式驗證。

- 派單改用全新 `claim_order_v2` JSON RPC，徹底避開 PostgREST 單一物件 coercion 與舊 schema cache 衝突。
- 訂單與司機查詢改為安全的 maybe-single 處理，找不到資料時回傳明確錯誤。

- 修正後台指定司機時 PostgREST 單一 JSON coercion 錯誤。
- 修正 LINE 訊息顯示 `\\n` 的問題。
- 強化台灣本島與常用地標驗證，排除國外、外島及矛盾地點。
- 管理後台拆分未完成與已完成訂單。
- 加入一般單與預約單接單限制。
- 車資使用 Google Maps 替代路線中符合計價規則的最優路線。
- 司機端加入 GPS 派單：Google 最佳駕車時間 20 分鐘內才顯示。
- GPS 派單不使用直線距離算法，並加入必要性判斷與快取以控制 API 用量。
- 整理正式部署文件與 Railway 環境變數範例。
